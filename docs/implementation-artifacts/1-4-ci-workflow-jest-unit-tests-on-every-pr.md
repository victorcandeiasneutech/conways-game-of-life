# Story 1.4: CI Workflow — Jest Unit Tests on Every PR

Status: ready-for-dev

## Story

As the candidate,
I want the CI workflow to run Jest across affected projects on every PR into `main`,
so that simulation rule regressions cannot merge undetected.

## Acceptance Criteria

1. **Given** `.github/workflows/ci.yml` has a `test` job,
   **When** a PR is opened or updated,
   **Then** the job runs `pnpm nx affected -t test --base=origin/main --parallel=3` and reports a named check status.
   **And** test output (pass/fail counts, failure messages) is visible in the GitHub Actions logs.

2. **Given** a PR introduces a failing Jest test in any affected project,
   **When** CI runs,
   **Then** the `test` check fails and blocks merge.

## Tasks / Subtasks

- [ ] Add `test` job to `.github/workflows/ci.yml` (AC: #1)
  - [ ] Follow the same job shape as `lint` and `typecheck` (checkout, pnpm setup, node setup, install, nx command)
  - [ ] Run `pnpm nx affected -t test --base=origin/main --parallel=3`
  - [ ] Confirm job is a separate named check (not merged into `lint` or `typecheck`)
- [ ] Open PR and confirm `test` check appears in the PR's checks tab (AC: #1, #2)

## Dev Notes

### What changes in this story

Story 1.3 left `ci.yml` with `lint` and `typecheck` jobs. This story adds the `test` job. The file will then have three of the four required jobs; `e2e` comes in story 1.5.

### Target `test` job to add to `ci.yml`

Add this job block alongside the existing `lint` and `typecheck` jobs:

```yaml
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 9.8.0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm nx affected -t test --base=origin/main --parallel=3
```

### Current Jest target state (important for implementation)

- **`apps/web`** — has `test` target with Jest (generated by `@nx/next` preset). `jest.config.cts` exists.
- **`libs/sim`, `libs/types`, `libs/ui`, `libs/api-client`** — do NOT have a `test` target yet. The `@nx/js:lib` and `@nx/react:lib` generators were run without `--unitTestRunner=jest`, so no Jest config was generated.

This is intentional: `libs/sim` tests (stories 2.1–2.4) will add Jest config via `@nx/jest:configuration` in the same PR as the first test specs. The `test` CI job is in place now so it picks them up automatically when story 2.1 lands.

On this PR, `pnpm nx affected -t test --base=origin/main --parallel=3` will only affect `apps/web` (if changed). Since no spec files exist in `apps/web` yet, the run produces a passing but empty result — which is correct behavior.

### Why `--parallel=3`

Architecture §7.2 specifies `--parallel=3`. This allows up to three Jest projects to run concurrently on the CI runner, reducing wall-clock time as the test suite grows (stories 2.x add sim tests; stories 3.x add web integration tests).

### Version pins (unchanged from story 1.3)

- pnpm: `9.8.0`
- Node: `20`
- `actions/checkout@v4`, `pnpm/action-setup@v4`, `actions/setup-node@v4`

### References

- [Source: docs/planning-artifacts/architecture.md#4.10] — Four required CI jobs; Job 3 is `test`
- [Source: docs/planning-artifacts/architecture.md#7.2] — `pnpm nx affected -t test --base=origin/main --parallel=3`
- [Source: docs/planning-artifacts/epics.md#Story-1.4] — ACs for the test job
- [Source: docs/project-context.md#5] — CI command shape
- [Source: .github/workflows/ci.yml] — File being extended

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

### Completion Notes List

### File List
